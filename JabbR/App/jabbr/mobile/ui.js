/*global define, document, window, setTimeout*/
define([
    'jabbr/base/ui',
], function (UI) {
    return UI.extend({
        constructor: function () {
            this.base();
        }
    });
});